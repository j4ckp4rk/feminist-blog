from django.shortcuts import render

# Create your views here.

def index(request):
    # 일단 def 되어있는데 리턴이 없으면 에러가 남. 그래서 임시로 pass 적용
    pass

def detail(request):
    # 일단 def 되어있는데 리턴이 없으면 에러가 남. 그래서 임시로 pass 적용
    pass

# def about(request):
#     # 일단 def 되어있는데 리턴이 없으면 에러가 남. 그래서 임시로 pass 적용
#     pass
